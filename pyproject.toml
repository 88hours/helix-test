
  [build-system]
  requires = ["setuptools"]
  build-backend = "setuptools.backends.legacy:build"

  [project]
  name = "helix-test"
  version = "0.1.0"
  dependencies = []

  [project.optional-dependencies]
  dev = ["pytest"]
